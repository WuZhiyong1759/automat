<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pt.mapper.StuMapper">

	<select id="findBysstatue" parameterType="int" resultType="com.pt.domain.Stu">
			SELECT * FROM stu WHERE sStatus > #{value}		
	</select>

</mapper>